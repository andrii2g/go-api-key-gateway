# Integration Guide

This repository is designed as a copy-first implementation kit. Copy `apikey/` and the pieces of `storage/sqlstore/`, `config/`, or `httpapi/` that your service needs, then replace imports with your own module path.
